{"v":1,"lamport":3,"session":"4a23a643-72bd-4add-8b03-cbe4379168c6","ops":[{"kind":"set_body","body":"Since daemonkit v0.20.9 every home-derived durable path resolves through the\npasswd database (internal/realhome.Dir) and ignores $HOME, so a test that only\ndoes t.Setenv(\"HOME\", dir) no longer sandboxes daemonkit-backed state — the\nlaunchd plist dir, the stable program root (~/.daemonkit/bin), and the artifact\nstore all land in the real home.\n\ndaemonkit exposes DAEMONKIT_HOME (realhome.EnvOverride) as the test seam; it is\nread by name at every call, never memoized, so t.Setenv works per test.\n\nRule: never t.Setenv(\"HOME\", dir) alone. Use internal/testhome — testhome.Temp(t)\nfor a fresh t.TempDir home, testhome.Pin(t, dir) for a caller-owned dir — which\nsets HOME and DAEMONKIT_HOME to the same directory. HOME still matters because\nnon-daemonkit code reads it.\n\nTwo of the escapes had no home seam at all, and predate v0.20.9:\nTestLauncherCarriesExactDaemonkitIdentity (internal/cli/root_test.go) and\nTestRuntimeIdentityIsExact (internal/runtimeconfig/runtime_test.go) reach\nservice.StableProgram via launcher()/Agent(), which stages the running test\nbinary at \u003chome\u003e/.daemonkit/bin/cc-review. That is what overwrote the real\ninstalled binary with a 37MB test binary. Any new test touching launcher(),\nruntimeconfig.Agent(), or paths.App() needs testhome. Fixed in 52c1049.\n\nRegression check: run the suite with DAEMONKIT_HOME pointed at a fresh scratch\ndir and assert that dir stays empty afterwards. A file appearing there means a\ntest is missing its per-test seam and would have written the real home.\n\nNever export DAEMONKIT_HOME process-wide in CI or shell config: cc-orchestrate\ninstall-binary.sh uses the same variable name with a different meaning."},{"kind":"add_anchor","anchor":{"kind":"path","value":"internal/cli/root_test.go"}},{"kind":"add_anchor","anchor":{"kind":"path","value":"internal/runtimeconfig/runtime_test.go"}}]}